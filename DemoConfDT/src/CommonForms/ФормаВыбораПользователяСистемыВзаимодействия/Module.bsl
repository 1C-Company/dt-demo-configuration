
&НаКлиенте
Процедура СписокВыборЗначения(Элемент, Значение, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	ВыбранныеЗначения = Новый Массив;
	
	Для Каждого Строка Из Значение Цикл
		ДанныеСтроки = Элементы.Список.ДанныеСтроки(Строка);
		ВыбранныеЗначения.Добавить(ДанныеСтроки.ИдентификаторПользователяИБ);
	КонецЦикла;
	
	Закрыть(ВыбранныеЗначения);
	
КонецПроцедуры
